document.querySelectorAll('.reveal').forEach((el)=>{new IntersectionObserver((entries)=>{entries.forEach((e)=>e.target.classList.toggle('show',e.isIntersecting));},{threshold:.15}).observe(el)});
const f=document.getElementById('contactForm');const s=document.getElementById('formStatus');if(f){f.addEventListener('submit',async(e)=>{e.preventDefault();if(s)s.textContent='전송 중입니다.';const fd=new FormData(f);try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:fd.get('name'),email:fd.get('email'),message:fd.get('message')})});if(!r.ok)throw new Error();f.reset();if(s)s.textContent='문의가 전송되었습니다. 확인 후 연락드리겠습니다.'}catch(err){if(s)s.textContent='전송 중 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'}})}